---
"@commerce-klaus/sfcc-module-resolver": patch
---

Split `src/index.ts` into focused internal modules (`cartridge-order.ts`, `module-resolution.ts`, `super-module.ts`, `hooks.ts`) re-exported from `index.ts`. No public API changes.
