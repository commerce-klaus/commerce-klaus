---
"@commerce-klaus/eslint-config-sfcc": minor
---

Add the opt-in `sfcc/no-controllers` rule and composable Storefront Next, PWA, SFRA, SiteGenesis Controllers, and SiteGenesis Pipelines policy presets. The `createStorefrontConfig()` helper can apply a policy to all or selected cartridges in mixed-architecture repositories.
