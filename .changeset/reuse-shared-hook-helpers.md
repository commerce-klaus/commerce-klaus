---
"@commerce-klaus/typescript-sfcc": patch
---

Use the shared `@commerce-klaus/sfcc-module-resolver` hook registration helpers instead of duplicating hook parsing, script resolution, and export-name inference logic.
