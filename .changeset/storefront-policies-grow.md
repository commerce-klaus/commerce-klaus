---
"@commerce-klaus/eslint-config-sfcc": minor
---

Expand the storefront architecture presets with explicit Pipeline API, SFRA server, and ISML rendering policies. Headless presets reject all three features, SFRA rejects Pipeline API access, and SiteGenesis presets reject the SFRA server module.
