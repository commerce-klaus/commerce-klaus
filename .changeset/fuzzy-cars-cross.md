---
"@commerce-klaus/typescript-sfcc": patch
---

Fix SFCC compatibility typings generated by `sfcc-ts-sync-types`.

- add dynamic index signature support for `dw.system.PipelineDictionary` so legacy access patterns like `pdict.Status` type-check again
- add dynamic key access support for `dw.util.Map` so usages like `request.httpParameters.format[0]` type-check in JavaScript projects
- keep existing `dw.web.HttpParameterMap` compatibility behavior and extend regression coverage
