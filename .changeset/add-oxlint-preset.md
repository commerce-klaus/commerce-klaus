---
"@commerce-klaus/eslint-config-sfcc": minor
---

Add an Oxlint preset and plugin subpath exports. The preset runs compatible SFCC and SiteGenesis rules in Oxlint, while `eslintAfterOxlint` provides a focused ESLint follow-up for rules Oxlint cannot run.
