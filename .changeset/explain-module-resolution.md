---
"@commerce-klaus/b2c-plugin": minor
"@commerce-klaus/sfcc-module-resolver": minor
---

Add `b2c klaus explain` and a shared resolution trace API that report the
effective cartridge order, containing cartridge, every attempted file path,
the selected match, and `module.superModule` fallback lookup.
