---
"@commerce-klaus/eslint-config-sfcc": minor
---

Add the opt-in `sfcc/no-dw-api` rule for portable cartridges. It rejects static `dw/*` imports and supports allow-listing exact modules or namespaces ending in `/*`.
