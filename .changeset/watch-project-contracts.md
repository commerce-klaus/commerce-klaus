---
"@commerce-klaus/b2c-plugin": minor
---

Add `b2c klaus validate --watch` for automatically revalidating SFCC hooks, job
steps, and Custom API contracts when cartridge scripts or metadata change.
