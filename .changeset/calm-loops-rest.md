---
"@commerce-klaus/eslint-config-sfcc": patch
---

Disable `unicorn/no-for-each` to prevent unsafe `for...of` replacements for SFCC host objects, and document why SFCC compatibility overrides are required.
