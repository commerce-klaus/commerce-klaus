---
"@commerce-klaus/eslint-config-sfcc": minor
---

Add `compatibility-21.2` and `compatibility-22.7` presets for projects targeting older SFCC compatibility modes. Keep the recommended config aligned with the current Script API and prevent Unicorn from suggesting unsupported runtime APIs or syntax.
