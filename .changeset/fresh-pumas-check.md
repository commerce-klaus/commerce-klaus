---
"@commerce-klaus/eslint-config-sfcc": patch
---

Fix `sfcc/no-empty-global` suggestions so they replace the full `empty(...)` call and include suggestions for identifier/member-expression arguments.
