---
"@commerce-klaus/eslint-config-sfcc": minor
---

Add `sfcc/prefer-tilde-require-path` to enforce `~/` imports for modules in the current cartridge, and enable it in the PWA and Storefront Next presets.
