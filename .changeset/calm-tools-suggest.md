---
"@commerce-klaus/eslint-config-sfcc": patch
---

Require named cartridge imports instead of `*/` lookup in the PWA and Storefront Next presets, and suggest an explicit import when `sfcc/no-proprietary-module-syntax` finds exactly one matching cartridge.
