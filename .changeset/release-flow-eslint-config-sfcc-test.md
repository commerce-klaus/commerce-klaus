---
"@commerce-klaus/eslint-config-sfcc": patch
---

Trigger a patch release for `@commerce-klaus/eslint-config-sfcc` to validate the release workflow (publish, tagging, and GitHub release creation).
