---
"@commerce-klaus/typescript-sfcc": patch
---

Keep CLI launcher files available before the package build so workspace installs can create `sfcc-ts-sync-types` and `sfcc-ts-typecheck` bin shims from a clean checkout.
